== Rails Chat Buckts

* Ruby version
  - 2.4.0 -> `gem install bundler`

* Configuration
  - `bundle install`

* System dependencies
  - imagemagik

* Database creation
  - `bundle exec rake db:migrate:reset`
