<%= form_for(@card) do |f| %>
  <%= f.text_field :body, placeholder: "Sentence", autofocus: true %>
  <%= f.text_field :translation, placeholder: "Translation" %>
  <%= f.submit "Save", class: "btn" %>
<% end %>
