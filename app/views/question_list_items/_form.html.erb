<%= form_for([@question_list, @question_list.question_list_items.build]) do |f| %>
  <%= f.text_field :content, placeholder: "New question", autocomplete: "off" %>
  <%= f.submit "Add", class: "btn" %>
<% end %>
